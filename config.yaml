vcf: "data/raw/test.vcf"

outputs:
  variants: "data/processed/variants.tsv"
  genotype_matrix: "data/processed/genotype_matrix.tsv"
  clusters: "results/clusters/clusters.tsv"
  pca_plot: "results/figures/pca_clusters.pdf"

clustering:
  n_clusters: 3
  random_seed: 42